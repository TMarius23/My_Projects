CREATE TABLE ITEM (
                      ID BIGINT PRIMARY KEY,
                      NAME VARCHAR(255),
                      DESCRIPTION VARCHAR(255),
                      STATUS VARCHAR(255),
                      EMAIL VARCHAR(255)
);
